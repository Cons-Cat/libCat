// -*- mode: c++;-*-
#pragma once

constexpr static int EXIT_SUCCESS = 0;  // NOLINT
constexpr static int EXIT_FAILURE = 1;  // NOLINT

using size_t = long unsigned int;
using usize = size_t;
using ssize_t = long signed int;
using isize = ssize_t;
