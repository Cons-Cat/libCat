// -*- mode: c++ -*-
// vim: set ft=cpp:
#pragma once

#include <concepts>
#include <type_traits>

namespace meta {

template <typename T>
constexpr auto bit_cast(auto& from_value) -> T;

}  // namespace meta

#include "./implementations/bit_cast.tpp"
